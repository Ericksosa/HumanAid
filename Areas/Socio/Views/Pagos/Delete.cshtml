@model Pago

@{
    ViewData["Title"] = "Eliminar Pago";
}

<div class="container-fluid py-4">
    <h1 class="mb-1">Eliminar Pago</h1>
    <p class="text-muted mb-4">Está a punto de eliminar permanentemente este pago</p>

    <div class="row">
        <div class="col-md-8">
            <div class="card border-danger mb-4">
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0">Confirmación de Eliminación</h5>
                </div>
                <div class="card-body">
                    <div class="alert alert-warning">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        <strong>¿Está seguro de que desea eliminar este pago?</strong>
                        <p class="mb-0">Esta acción no se puede deshacer. Una vez eliminado, toda la información asociada a este pago se perderá permanentemente.</p>
                    </div>

                    <div class="bg-light p-3 rounded mb-4">
                        <h5 class="mb-3">Información del pago a eliminar</h5>

                        <div class="row mb-2">
                            <div class="col-md-3 fw-bold">Socio:</div>
                            <div class="col-md-9">@Html.DisplayFor(model => model.Socio.Nombre)</div>
                        </div>

                        <div class="row mb-2">
                            <div class="col-md-3 fw-bold">Tipo de Cuota:</div>
                            <div class="col-md-9">
                                <span class="badge bg-info">@Html.DisplayFor(model => model.TipoCuota.Nombre)</span>
                            </div>
                        </div>

                        <div class="row mb-2">
                            <div class="col-md-3 fw-bold">Monto de Cuota:</div>
                            <div class="col-md-9">@Html.DisplayFor(model => model.MontoCuota)</div>
                        </div>

                        <div class="row mb-2">
                            <div class="col-md-3 fw-bold">Cantidad de Cuotas:</div>
                            <div class="col-md-9">@Html.DisplayFor(model => model.CantidadCuotas)</div>
                        </div>

                        <div class="row mb-2">
                            <div class="col-md-3 fw-bold">Fecha de Pago:</div>
                            <div class="col-md-9">@Html.DisplayFor(model => model.FechaPago)</div>
                        </div>

                        <div class="row mb-2">
                            <div class="col-md-3 fw-bold">Monto Total:</div>
                            <div class="col-md-9">
                                <span class="badge bg-success">@(Model.MontoCuota * Model.CantidadCuotas)</span>
                            </div>
                        </div>
                    </div>

                    <form asp-action="Delete" method="post">
                        <input type="hidden" asp-for="Id" />

                        <div class="d-flex">
                            <a asp-action="Index" class="btn btn-outline-secondary me-2">Cancelar y Volver</a>
                            <button type="submit" class="btn btn-danger">Eliminar Permanentemente</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="bg-light p-4 rounded">
                <h4>Información Importante</h4>
                <hr />
                <p>Antes de eliminar este pago, tenga en cuenta lo siguiente:</p>
                <ul class="mb-0">
                    <li class="mb-2">Esta acción no se puede deshacer</li>
                    <li class="mb-2">Los informes financieros se verán afectados</li>
                    <li class="mb-2">El historial de pagos del socio se actualizará</li>
                    <li class="mb-2">Se registrará esta acción en el sistema</li>
                </ul>
            </div>
        </div>
    </div>
</div>