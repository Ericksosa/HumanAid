@model HumanAid.Models.Socio

@{
    ViewData["Title"] = "Eliminar Socio";
}

<div class="container-fluid py-4">
    <h1 class="mb-1">Eliminar Socio</h1>
    <p class="text-muted mb-4">Está a punto de eliminar permanentemente este socio</p>

    <div class="row">
        <div class="col-md-8">
            <div class="card border-danger mb-4">
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0">Confirmación de Eliminación</h5>
                </div>
                <div class="card-body">
                    <div class="alert alert-warning">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        <strong>¿Está seguro de que desea eliminar este socio?</strong>
                        <p class="mb-0">Esta acción no se puede deshacer. Una vez eliminada, toda la información asociada a este socio se perderá permanentemente.</p>
                    </div>

                    <div class="bg-light p-3 rounded mb-4">
                        <h5 class="mb-3">Información del socio a eliminar</h5>

                        <div class="row mb-2">
                            <div class="col-md-3 fw-bold">Nombre:</div>
                            <div class="col-md-9">@Html.DisplayFor(model => model.Nombre)</div>
                        </div>

                        <div class="row mb-2">
                            <div class="col-md-3 fw-bold">Fecha de Nacimiento:</div>
                            <div class="col-md-9">@Model.FechaNacimiento.ToString("dd/MM/yyyy")</div>
                        </div>

                        <div class="row mb-2">
                            <div class="col-md-3 fw-bold">Cuenta Bancaria:</div>
                            <div class="col-md-9">@Html.DisplayFor(model => model.CuentaBancaria)</div>
                        </div>

                        <div class="row mb-2">
                            <div class="col-md-3 fw-bold">Tipo de Cuota:</div>
                            <div class="col-md-9">@Html.DisplayFor(model => model.TipoCuota.Nombre)</div>
                        </div>

                        <div class="row mb-2">
                            <div class="col-md-3 fw-bold">Sede:</div>
                            <div class="col-md-9">@Html.DisplayFor(model => model.Sede.Nombre)</div>
                        </div>

                        <div class="row mb-2">
                            <div class="col-md-3 fw-bold">Usuario:</div>
                            <div class="col-md-9">@Html.DisplayFor(model => model.Usuario.Correo)</div>
                        </div>
                    </div>

                    <form asp-action="Delete" method="post">
                        <input type="hidden" asp-for="SocioId" />

                        <div class="d-flex">
                            <a asp-action="Index" class="btn btn-outline-secondary me-2">Cancelar y Volver</a>
                            <button type="submit" class="btn btn-danger">Eliminar Permanentemente</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="bg-light p-4 rounded">
                <h4>Información Importante</h4>
                <hr />
                <p>Antes de eliminar este socio, tenga en cuenta lo siguiente:</p>
                <ul class="mb-0">
                    <li class="mb-2">Se eliminarán todos los registros asociados</li>
                    <li class="mb-2">Esta acción no se puede deshacer</li>
                    <li class="mb-2">Los informes históricos se verán afectados</li>
                    <li class="mb-2">El usuario asociado no será eliminado</li>
                </ul>
            </div>
        </div>
    </div>
</div>