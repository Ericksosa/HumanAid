@model IEnumerable<HumanAid.Models.Alimento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Envio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Peso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Envio.Destino)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Peso)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AlimentoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AlimentoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AlimentoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
