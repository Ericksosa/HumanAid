@model HumanAid.Models.Medicamento

@{
    ViewData["Title"] = "Delete";
}

<div class="container mb-5">
    <!-- Header Section -->
    <div class="row mb-4">
        <div class="col-12">
            <h1 class="display-5 fw-bold">Eliminar Donación de Medicamentos</h1>
            <p class="text-muted">Está a punto de eliminar permanentemente esta donación de medicamentos</p>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-8">
            <!-- Warning Card -->
            <div class="card border-danger border-2 shadow-sm mb-4">
                <div class="card-header bg-danger text-white py-3">
                    <div class="d-flex align-items-center">
                        <i class="bi bi-exclamation-triangle-fill fs-4 me-2"></i>
                        <h5 class="card-title mb-0">Confirmación de Eliminación</h5>
                    </div>
                </div>
                <div class="card-body p-4">
                    <div class="alert alert-warning">
                        <div class="d-flex">
                            <i class="bi bi-info-circle-fill me-2 fs-5"></i>
                            <div>
                                <h6 class="alert-heading">¿Está seguro de que desea eliminar este medicamento?</h6>
                                <p class="mb-0">Esta acción no se puede deshacer. Una vez eliminado, toda la información asociada a este medicamento se perderá permanentemente.</p>
                            </div>
                        </div>
                    </div>

                    <div class="card bg-light mt-4 mb-4">
                        <div class="card-body">
                            <h5 class="card-title mb-3">Información del medicamento a eliminar</h5>

                            <div class="row g-3">
                                <!-- Nombre del Medicamento -->
                                <div class="col-md-6">
                                    <div class="d-flex align-items-center">
                                        <i class="bi bi-prescription2 text-primary fs-4 me-3"></i>
                                        <div>
                                            <div class="text-muted small">
                                                @Html.DisplayNameFor(model => model.Nombre)
                                            </div>
                                            <div class="fw-bold fs-5">
                                                @Html.DisplayFor(model => model.Nombre)
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <!-- Dosis -->
                                <div class="col-md-6">
                                    <div class="d-flex align-items-center">
                                        <i class="bi bi-droplet-half text-info fs-4 me-3"></i>
                                        <div>
                                            <div class="text-muted small">
                                                @Html.DisplayNameFor(model => model.Dosis)
                                            </div>
                                            <div class="fw-bold fs-5">
                                                @Html.DisplayFor(model => model.Dosis)
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <!-- Cantidad -->
                                <div class="col-md-6">
                                    <div class="d-flex align-items-center">
                                        <i class="bi bi-box-seam text-success fs-4 me-3"></i>
                                        <div>
                                            <div class="text-muted small">
                                                @Html.DisplayNameFor(model => model.cantidad)
                                            </div>
                                            <div class="fw-bold fs-5">
                                                @Html.DisplayFor(model => model.cantidad) unidades
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <!-- Destino del Envío -->
                                <div class="col-md-6">
                                    <div class="d-flex align-items-center">
                                        <i class="bi bi-truck text-warning fs-4 me-3"></i>
                                        <div>
                                            <div class="text-muted small">
                                                @Html.DisplayNameFor(model => model.Envio)
                                            </div>
                                            <div class="fw-bold fs-5">
                                                @Html.DisplayFor(model => model.Envio.Destino)
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <form asp-action="Delete" class="mt-4">
                        <input type="hidden" asp-for="MedicamentoId" />
                        <div class="d-flex justify-content-between align-items-center">
                            <a asp-action="Index" class="btn btn-outline-secondary">
                                <i class="bi bi-arrow-left me-2"></i>Cancelar y Volver
                            </a>
                            <button type="submit" class="btn btn-danger btn-lg" onclick="return confirm('¿Está completamente seguro de eliminar este medicamento?');">
                                <i class="bi bi-trash me-2"></i>Eliminar Permanentemente
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-4">
            <!-- Info Card -->
            <div class="card border-0 bg-light mb-4">
                <div class="card-body p-4">
                    <h5 class="card-title">
                        <i class="bi bi-info-circle me-2"></i>Información Importante
                    </h5>
                    <p class="card-text">Antes de eliminar este medicamento, tenga en cuenta lo siguiente:</p>

                    <ul class="list-group list-group-flush bg-transparent mb-3">
                        <li class="list-group-item bg-transparent border-0 ps-0">
                            <i class="bi bi-dash-circle text-danger me-2"></i>
                            <span>Se eliminarán todos los registros asociados</span>
                        </li>
                        <li class="list-group-item bg-transparent border-0 ps-0">
                            <i class="bi bi-dash-circle text-danger me-2"></i>
                            <span>Esta acción no se puede deshacer</span>
                        </li>
                        <li class="list-group-item bg-transparent border-0 ps-0">
                            <i class="bi bi-dash-circle text-danger me-2"></i>
                            <span>Los informes de inventario se verán afectados</span>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
