@model IEnumerable<HumanAid.Models.VoluntarioAdministrativo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Profesion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Departamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Voluntario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Profesion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Departamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Voluntario.Direccion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VoluntarioAdministrativoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VoluntarioAdministrativoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VoluntarioAdministrativoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
